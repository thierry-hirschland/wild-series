{% extends "base.html.twig" %}

{% block body %}
    <h1> Ajout d'une nouvelle catégories : </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type='submit' value='Add category'>
    {{ form_end(form) }}
    <a href="{{ path('category_index') }}">
    Retour à la liste des catégories
</a>
{% endblock %}